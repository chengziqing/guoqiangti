filesets:
    includes:
    excludes:
        - \.git
        - ngrok
        - .gobuild.yml
        - README.md
settings:
        build: |
            if test "$GOOS" = "windows" -a "$GOARCH" = "386"
            then
                go install -v -tags 'release'
            else
                go install -v -tags 'release'
            fi